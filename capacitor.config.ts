import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pokemon.pokeapi.com',
  appName: 'Pokemon App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
